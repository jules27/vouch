- if @friends.count == 0
  You don't have any friends.
  = add_friend_link
- else
  %div
    Search form here...

  - @friends.each do |friend|
    - friend.restaurant_lists_by_city(@city.name).each do |list|
      %h4
        = capitalize_title(list.title)

      %div
        %table.table.table-striped.table-condensed
          %thead
            %tr
              %th
                Restaurants
              %th
                Neighborhoods
              %th
                Yelp Rating
              %th
                Reviews
              %th
                Tags
              %th
          %tbody
            - list.vouch_items.each do |item|
              - b = Business.find(item.business_id)
              %tr
                %td
                  = b.name
                %td
                  = b.neighborhood
                %td
                  %span{ class: "rating-static rating-#{rating_class(b.yelp_rating)}" }
                %td
                  = b.yelp_review_count
                %td
                  = item.tag_list
                %td
                  = link_to "Add to \"Places I Want to Go\"", "#", class: "btn btn-primary btn-small"