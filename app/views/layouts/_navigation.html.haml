.navbar.navbar-fluid-top
  .navbar-inner
    .container-fluid
      %a.btn.btn-navbar{ 'data-target' => ".nav-collapse", 'data-toggle' => "collapse" }
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.brand{ href: root_path } Vouch
      .container-fluid.nav-collapse
        %ul.nav
          - if user_signed_in?
            %li
              = link_to "My Vouches", vouch_lists_path
            %li
              = link_to "My Friend's Vouches", "#"
            %li
              = link_to "Places I Want To Go", "#"
        %ul.nav.pull-right
          - if user_signed_in?
            %li.dropdown
              %a{ :href => "#", :class => "dropdown-toggle", "data-toggle" => "dropdown" }
                Make New Vouches
                %b.caret
              %ul.dropdown-menu
                %li
                  - City.all.each do |city|
                    = link_to city.name, new_vouch_list_city_path(city.name)
            %li.dropdown
              %a{ :href => "#", :class => "dropdown-toggle", "data-toggle" => "dropdown" }
                #{current_user.first_name} #{current_user.last_name}
                %b.caret
              %ul.dropdown-menu
                %li
                  = link_to current_user.email, "#", class: "navbar-email"
                %li.divider
                %li
                  = link_to "Sign Out", destroy_user_session_path, method: :delete
          - else
            -# %li
            -#   = facebook_login_link
            %li
              = link_to "Sign Up", new_user_registration_path
            %li
              = link_to "Sign In", new_user_session_path
